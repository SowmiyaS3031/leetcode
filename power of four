class Solution {
    public boolean isPowerOfFour(int n) {
        if (n <= 0) return false;

        // check power of 2  (only one bit set)
        if ((n & (n - 1)) != 0) return false;

        // check if that bit is in even position
        return (n & 0x55555555) != 0;
    }
}
